<!--index.wxml-->
<view id='main' scroll-y="true">
  <view wx:for="{{docs}}" class="flex-align-center" >

    <image class="icon" mode="aspectFit" src="../../assets/images/fold.png"></image>
    <view id="{{item.id}}" data-n='{{item.name}}' class="btn" bindtap='tapShow'>{{item.name}}</view>

  </view>

</view>
